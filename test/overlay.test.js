var Overlayer = require('../src/layers/overlay.js');
var assert = require('assert');

describe('test Overlayer', function(){
    it('it should return Overlayer instance,', function() {
        var overlay = new Overlayer(opts);
        assert. overlay.setMap({});
    })
})
